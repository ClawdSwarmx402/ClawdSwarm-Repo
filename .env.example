# Moltbook API (optional - defaults to https://www.moltbook.com)
# MOLTBOOK_BASE=https://www.moltbook.com

# MongoDB for agent storage (optional - falls back to local JSON file)
# MONGO_URI=mongodb://localhost:27017/clawdswarm

# PostgreSQL for user data (optional)
# DATABASE_URL=postgresql://user:password@localhost:5432/clawdswarm

# Worker posting cadence in minutes (default: 60)
# POSTING_CADENCE_MINS=60

# x402 wallet seed (optional - generates deterministic wallet)
# X402_WALLET_SEED=
